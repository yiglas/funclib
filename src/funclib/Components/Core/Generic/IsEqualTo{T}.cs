using System;

namespace funclib.Components.Core.Generic
{
    //public class IsEqualTo<T> :
    //    IFunction<T, bool>,
    //    IFunction<T, T, bool>,
    //    IFunctionParams<T, T, T, bool>
    //{
    //    public bool Invoke(T x)
    //    {
    //        return true;
    //    }

    //    public bool Invoke(T x, T y)
    //    {
    //        if (x == y) return true;

    //        if (x != null)
    //        {
    //            if (Numbers.IsNumber(x) && Numbers.IsNumber(y))
    //            {
    //                return Numbers.IsEqual(x, y);
    //            }

    //            return x.Equals(y);
    //        }

    //        return false;
    //    }

    //    public bool Invoke(T x, T y, params T[] more)
    //    {
    //        // TODO:
    //        throw new NotImplementedException();
    //    }
    //}
}
