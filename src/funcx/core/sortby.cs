namespace funcx
{
    using System;
    using System.Collections;
    using System.Collections.Generic;
    using System.Linq;
    using System.Text;

    public static partial class core
    {
        //public static IList<T> sortby<T>(Func<T, bool> comp, IEnumerable<T> coll) =>
        //    sort(comp, coll);

        //public static IList<T> sortby<T>(Func<T, T> keyfn, Func<T, T, bool> comp, IEnumerable<T> coll) =>
        //    sort((x, y) => comp(keyfn(x), keyfn(y)), coll);
    }
}
